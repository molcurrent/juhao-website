# JUHAO 钜豪照明官网交接摘要

- 最后核验：2026-07-13 19:27（Asia/Shanghai）
- 项目目录：`/Users/mac/Documents/Codex/2026-07-12/sites-plugin-sites-openai-bundled-3`
- 当前分支：`main`
- 网站实施基线：`01305b088d047fc33e4e7d3e9fe0461d0ff060aa`
- 当前私有线上应用提交：`4135b81830e8442bcfb643ea8fa130a0603d415d`
- 本轮状态：代码与内容验收完成，Sites 第 18 版私有部署待执行

> 本文档是当前项目事实的优先入口。旧版 `README.md`、`NOTES.md` 和部分 `RECON/**` 记录只代表历史阶段；数量、发布状态和联系方式必须以当前代码、统一发布台账、媒体台账、Sites 实时状态和本文档为准。

## 1. 当前结论

本轮已经把首页页脚的 22 个不重复目标路径纳入统一发布治理，并根据本地企业知识库完善产品、场景方案、案例、企业动态、照明知识、搜索、咨询和技术 SEO。当前私有预览具备以下能力：

- 页脚目标均有真实页面、返回上级内链和下一步业务入口。
- 99 款来源商品候选中，31 款通过当前私有发布门禁。
- 10 个产品专题、6 个场景方案页面、6 个案例、20 篇内容文章进入统一台账。
- 站内搜索只读取统一台账中的可搜索记录。
- sitemap、Schema 和公开索引都受同一人工审核与媒体授权门禁控制。
- 咨询表单可真实写入 D1、生成线索编号并核验成功页。
- 未核验电话、邮箱、企业微信、项目完工状态和远程图片不再被当作已确认事实发布。

当前仍是私有预览，不是正式公开上线。163 条台账记录中 95 个路由可在私有站访问、80 个可站内搜索、0 个允许搜索引擎索引；`sitemap.xml` 有效但不输出 `<loc>`。

## 2. 统一发布台账

### 2.1 当前规模

| 指标 | 数量 | 解释 |
| --- | ---: | --- |
| 发布台账记录 | 163 | 页面、专题、产品、案例、文章和分页的统一治理记录 |
| 私有已发布路由 | 95 | 仅表示私有预览可访问，不等于公开 SEO 发布 |
| SEO 候选 | 83 | 内容结构已达到候选标准，仍需人工签核 |
| 站内可搜索路由 | 80 | 与 sitemap 状态解耦 |
| 公开可索引路由 | 0 | 私有预览门禁的预期结果 |
| 产品候选 | 99 | 来自有效商城商品记录 |
| 私有发布产品 | 31 | 来源字段通过机器校验；业务审核与图片授权仍待签核 |
| 产品专题 | 10 | 智能家居设备专题当前为 0 款审核产品，保持不可索引 |
| 场景方案页 | 6 | 总览加住宅、酒店、商业、公共、工业五类场景 |
| 工程案例 | 6 | 均保留来源中的项目阶段与证据边界 |
| 企业动态 | 8 | 来自有效商城资讯记录，使用真实来源日期 |
| 照明知识 | 12 | 使用已审核知识包，不把审核日冒充发布日期 |
| 资讯分页 | 3 | `/news/page/2` 至 `/news/page/4` |

核心文件：

- `content/publication-ledger.ts`
- `content/governance/content-ledger.csv`
- `content/governance/content-ledger.json`
- `content/governance/manual-approval-queue.csv`
- `content/search-index.ts`
- `app/sitemap.ts`
- `scripts/validate_content_ledger.mjs`

### 2.2 索引门禁

页面只有同时满足以下条件，才允许进入 sitemap、输出页面级 Schema 并移除 `noindex`：

1. 页面处于可发布状态。
2. 事实状态通过人工审核。
3. 审核人和审核日期已记录。
4. 媒体公开授权已确认。
5. 页面被明确批准为 SEO 索引候选。
6. 公共索引环境开关已显式启用。

任何一项不满足，页面仍可用于私有审核，但保持 `noindex, follow`，不输出容易被搜索引擎误判为正式发布的页面级结构化数据。

## 3. 本地企业知识库

### 3.1 已修复和验证

- 有效商品规则：`isSale=1`、`goodsStatus=1`、`dataFlag=1`。
- 有效商城商品：1,919 条；加 3 个本地补充页后商品说明共 1,922 条。
- 分类索引 27 个、专题索引 21 个、部门索引 12 个。
- 有效帮助文章 137 篇，包含文章 ID 232；8 条软删除记录未进入发布链。
- 帮助文章保留创建日期、封面、摘要和跳转地址原始字段；源数据缺失时明确记为缺失，不补造。
- 全库 Markdown 文件 2,161 个。
- Wiki 引用 6,811 条，失效 0、歧义 0。
- 同一输入连续生成结果稳定，知识库树哈希：`c8f532ce24a3857440d8d799aedf45a490bbc16fbb532fccaa639b79e1910f24`。

源数据仍有真实缺口：137 篇有效文章中 34 篇缺封面、26 篇缺摘要、131 篇缺跳转地址。这些是源数据问题，不应由网站自动虚构补齐。

### 3.2 恢复点

- 知识库归档：`/Users/mac/Documents/Codex/2026-07-13/juhao-website-handoff-md-users-mac/work/kb-p0/kb-before-week1.tar.gz`
- 大小：206 MB
- 商城 SQL：`/Users/mac/Documents/juhao数据库/juhao_mall_2026-07-10_02-41-52_mysql_data.sql`
- SQL SHA-256：`c9ae607cb66d6302555621724d491e2dfc9986d582479be1fbb5691cc05201a8`

知识库后续重建必须继续保留 3 个 `_本地补充.md`，先在临时目录验证数量、链接和哈希，再写回正式 `企业知识库`。

## 4. 产品与专题

### 4.1 产品发布口径

- “来源字段已通过机器校验”不等于企业已经核实性能、兼容、库存或售后。
- 商品唯一来源 ID 用作 `sku`；型号只作为型号或 `mpn` 字段。
- 私有预览产品页不输出 `Product` Schema。
- 产品图片公开使用授权仍待核验，因此远程图片不进入可索引状态。
- 采用“审核一款、发布一款”，不等待 100 款一次性齐全。

### 4.2 三个旗舰专题

- 射灯与轨道照明：6 款代表产品、空间场景、参数选择、知识、案例、FAQ 和咨询入口已连接。
- 家居顶灯：6 款代表产品、空间场景、参数选择、知识、家庭应用、FAQ 和咨询入口已连接。
- 智能家居设备：方法内容、知识和咨询路径已完成；当前 0 款产品通过门禁，因此不补造协议、兼容、安装或售后能力，继续不可索引。

其他专题沿用相同治理链，不继续批量制造结构相似的薄页面。

## 5. 案例证据链

当前案例只使用以下 6 条有效来源：`#226`、`#231`、`#228`、`#229`、`#220`、`#199`。

- 深圳华发冰雪世界 JW 万豪酒店：旗舰证据页已完成，保留签约/来源阶段，不虚构完工效果。
- 上饶广丰铂尔曼酒店：旗舰证据页已完成，保留来源阶段和资料缺口。
- 丽水银泰万豪酒店：按来源记录展示 8 个明确空间，不增加推断空间。
- 舟山普陀希尔顿欢朋酒店：来源未给空间分栏，页面明确不推断。
- 烟台君澜酒店：仅使用来源中的 4 张唯一图片候选，授权仍待复核。
- 邢台金融中心：按来源记录展示 8 个明确空间。

旧来源 `#225` 不再作为项目案例，旧案例路由返回 404；它只保留为荣誉来源记录。

每个案例页均把“来源确认事实”“当前项目阶段”“空间资料”“暂缺资料”“媒体授权状态”分开显示。项目图片未完成公开授权时，只能作为待审核资料候选，不能成为完工证明。

## 6. 新闻与照明知识

### 6.1 企业动态 8 篇

来源 ID：`#232`、`#224`、`#223`、`#222`、`#221`、`#219`、`#218`、`#217`。

- 保留商城 SQL 中的真实日期。
- 四条酒店记录只表述为“中标报道 / 后续实施待证”。
- 宣传性排名、客流、渠道效果和未核验项目进展不作为页面结论。
- 所有远程封面和正文图片继续锁定待授权。
- 页面使用本地原创栏目示意图，且明确标注“不作为活动或项目证据”。

### 6.2 照明知识 12 篇

- 筒灯与射灯区别
- 光束角与光斑
- 射灯洗墙离墙距离
- 色温选择
- 显色指数
- 分层照明
- LED 灯带设计与安装
- IES 配光文件
- 潮湿空间 IP 等级
- LED 调光兼容
- 频闪与时间光调制
- 恒压与恒流驱动

每篇均含知识来源、审核主体、审核日期、内容边界、本地语义化代表图、相关文章和咨询入口。知识包只确认审核日期，未给正式发布日期，因此 `published_at` 保持 `unknown`。

## 7. 媒体与结构化数据

媒体台账共有 489 条：

- 已批准本地媒体 35 条。
- 待审核 454 条。
- 远程媒体 445 条，全部不可发布、不可索引。
- 20 个真实文章路由都有已批准的本地栏目代表图。

关键产品、案例和文章图片使用真实 `<img>` / 语义化媒体结构，声明宽高、alt、图注、来源和授权状态。产品与案例的专属 OG 图在授权完成前使用安全的本地默认图；不把未授权远程图写入分享元数据。

页面级 `Article`、`Product`、`FAQPage`、`Service`、`WebPage` 和 `BreadcrumbList` Schema 受统一可索引门禁控制。私有预览仍保留基础 `Organization` / `WebSite`，且只包含已确认安全的名称和网址，不包含待核验联系方式和地址。

核心文件：

- `content/governance/media-inventory.csv`
- `content/governance/media-inventory.json`
- `scripts/build_media_inventory.py`
- `tests/media-inventory.test.mjs`

## 8. 咨询闭环

联系页保留两步流程：

1. 用户选择家庭、工程或渠道方向，填写项目概况、阶段和需求。
2. 检查通过后提交回访信息和隐私同意。

提交后：

- 保存 `source`、`sourceDetail`、`scene`、`intent`。
- 进行同源、正文大小、字段、蜜罐和幂等校验。
- 写入 D1 `consultation_leads`。
- 生成 `JUHAO-YYYYMMDD-XXXXXXXX` 线索编号。
- 成功页必须重新查询 D1；伪造编号不会显示“咨询已提交”。
- 若以后配置 HTTPS Webhook，可记录内部通知的 `sent / failed` 状态。

当前真实边界：

- 电话、邮箱和企业微信均显示“待企业核验 / 当前不可用”。
- 候选电话 `400-0760-888`、候选邮箱 `export@juhaolamp.com` 未获企业签核，已从页面和 Schema 移除。
- 生产环境尚未配置内部 Webhook，因此目前是“官网保存闭环”，不是完整的企业分发和处理闭环。
- 隐私政策、保存期限、删除流程仍需法务签核。

核心文件：

- `features/platform/ContactPage.tsx`
- `app/api/contact/route.ts`
- `app/contact/success/page.tsx`
- `db/consultation-leads.ts`
- `content/governance/manual-approval-queue.csv`

## 9. 技术 SEO

已完成：

- 每个真实页面有独立 title、description、canonical 和 robots 状态。
- 动态面包屑使用中文名称，不显示 slug 或裸数字。
- 站内搜索从发布台账生成，覆盖 80 个允许搜索的路由。
- sitemap 只读取统一发布台账；私有模式输出空 URL 集。
- `lastModified` 来自内容更新时间，不使用构建当天冒充。
- noindex 页面不进入 sitemap。
- 已确认的旧路由使用 308 重定向，垃圾 URL 返回 410。
- 法律、隐私、招聘、下载、搜索、联系成功页等按资料门禁保持 noindex。
- `PUBLIC_INDEXING_ENABLED=1` 是公开索引的必要但非充分条件；人工审核和媒体授权仍必须逐条通过。

公开切换前必须运行：

```bash
node scripts/validate_content_ledger.mjs
node scripts/check_launch_health.mjs
npm test
```

## 10. 验证结果

最后一次完整验证：2026-07-13。

- `npm test`：59/59 通过，包含生产构建。
- `./node_modules/.bin/tsc --noEmit`：通过。
- `npm run lint`：通过。
- `git diff --check`：通过。
- `node scripts/validate_content_ledger.mjs`：0 error、0 warning。
- 产品台账重建：99 候选、31 私有发布、6 案例、58 静态路由、0 可索引。
- 媒体台账重建：489 条、35 已批准、454 待审核、0 可索引。

真实浏览器验收：

- 390 × 844 首页、产品专题、旗舰案例、企业动态和联系页无横向溢出。
- 1012 × 900 首页保持精简桌面导航，未过早切换汉堡菜单。
- 移动菜单可开关、Escape 可关闭、焦点进入菜单。
- 联系页携带页脚来源上下文，第一步检查与第二步回访字段正常。
- 企业动态本地代表图加载成功，控制台 0 error。
- 1440 px 页脚有 26 个链接、23 个不重复路径（含首页），22 个非首页目标全部可达。

已知非阻断警告：客户端主包大于 500 kB，构建仅发出代码分割建议，不影响本轮验收；后续性能轮次可单独拆包。

## 11. Sites 私有部署状态

- 项目 ID：`appgprj_6a533dc64d64819194e7761cf915e12d`
- 当前私有地址：`https://juhao-lighting-2026.rocky-snail-3254.chatgpt.site`
- 当前访问策略：`custom`，仅所有者一人，无用户组。
- 当前线上版本：第 17 版。
- 本轮目标：保存并部署第 18 版私有版本。
- 正式域名和公开访问：本轮不变更。

本节在第 18 版部署完成后更新具体版本、提交和部署 ID。

## 12. 仍需企业人工完成的门禁

以下事项不能由代码或本地知识库替代：

1. 企业主体、成立年份、产能、门店数、服务区域和荣誉口径签核。
2. 电话、邮箱、企业微信和对应责任人签核。
3. 31 款产品的参数、兼容、在售状态、售后和公开图片授权审核。
4. 6 个案例的项目阶段、完工证据和图片公开授权审核。
5. 445 条远程媒体逐条授权或替换。
6. 法律声明、隐私政策、数据保存期限和删除流程法务签核。
7. 内部通知 Webhook 的地址、鉴权、接收人、重试和告警配置。
8. `mall.juhao.com` 稳定性、登录旧站行为和咨询兜底的正式验收。
9. `juhao.com` / `www.juhao.com` DNS、SSL、301、Analytics 和搜索平台账号。
10. 公开访问和搜索引擎索引的最终批准。

人工门禁未通过前，保持当前 `noindex`、空 sitemap 和所有者私有访问，不用“待完善”通用文案掩盖事实缺口。

## 13. 下一位执行者的安全顺序

1. 先查看 `content/governance/manual-approval-queue.csv`，收集企业签核结果。
2. 只更新通过签核的单条台账记录，不批量放开同类内容。
3. 重建产品和媒体台账并运行全部测试。
4. 私有 Sites 部署并抽检首页、专题、案例、资讯、联系页、robots 和 sitemap。
5. 公开前单独取得用户批准，再处理公开访问、正式域名和索引开关。

常用命令：

```bash
cd /Users/mac/Documents/Codex/2026-07-12/sites-plugin-sites-openai-bundled-3
python3 scripts/build_product_catalog.py
python3 scripts/build_media_inventory.py
node scripts/validate_content_ledger.mjs
npm test
./node_modules/.bin/tsc --noEmit
npm run lint
git diff --check
```
